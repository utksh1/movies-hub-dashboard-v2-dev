# UI Source Constraint

Use `old/modded_apk` as the only UI reference for web conversion.

- Include: layouts, styles, strings, and behavior from the modded APK.
- Exclude: UI decisions from `old/original_apk`.
- Goal: web UI should match the modded app experience, not the original app baseline.
