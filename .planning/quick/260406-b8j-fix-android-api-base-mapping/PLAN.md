# Quick Task Plan

- Use Firebase Remote Config to fetch live `SERVER_URL` and `API_KEY`.
- Normalize effective API base as `SERVER_URL + "android/"`.
- Probe priority endpoints (recent, search, details, stream links, live TV).
- Save results and lightweight schema snapshots to JSON for follow-up UI work.
